# ⚡Ball Game

Готовая playable-реклама, реализованная на PIXI.js (рендеринг) и Matter.js (физика).
Визуальная новелла в формате «натяни и стреляй» — собрана в единый автономный HTML-файл.

1.  **Готовый файл:** Находится в папке `dist/index.html`. Его можно запустить локально в любом браузере.
2.  **Исходники:** Расположены в папке `src/`.


## ⚙️ Как собрать и запустить

### 1. Клонирование репозитория
```bash
git clone https://github.com/alexeypiskunovich/ball-game.git
#Переход в директорию проекта
cd ball-game
```

### 🚀Запуск в режиме разработки:
```bash
#Установите зависимости: 
npm install
#Запуск:
npm run dev
```

### Сборка финального index.html:
```bash
#Выполните команду: 
npm run build
#Сборщик Vite с плагином `vite-plugin-singlefile` упакует JS, CSS и логику в файл `dist/index.html`.
```
## ⚙️ Конфигурация (Config)
Все игровые параметры вынесены в отдельный файл `src/config.ts`:
### 🎯 БАЛАНС УРОВНЯ
* winTimeout: 9000 — таймер на прохождение (9 секунд)
* minVelocityThreshold: 0.8 — остановка снаряда = поражение
###  🪀 ФИЗИКА СНАРЯДА
* maxDragDistance: 150 — предел натяжения рогатки
* maxLaunchForce: 0.025 — сила выстрела
* restitution: 0.75 — упругость (отскок)
* frictionAir: 0.01 — сопротивление воздуха
### 🧱 ПРЕПЯТСТВИЯ И ГРАНИЦЫ
* boundaryRestitution: 0.9 — отскок от стен
* Статичные блоки с параметром chamfer (скругление)


## 🤖 Использование AI-инструментов
При разработке использовался **DeepSeek AI**:
* Просчёт векторов натяжения и нелинейной силы броска.
* Настройка коллизий Matter.js под аркадную физику «натяни-и-стреляй».
* Синхронизация PIXI-спрайтов с Matter-телами без лагов.
